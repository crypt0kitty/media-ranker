<main>
  <section class="spotlight">
    <h2 class="spotlight__header">
      <span class="spotlight__header--prefix">Media Spotlight</span>
      <a class="spotlight__link-to" href="works/605.html"><%=@spotlight.description%></a> <br>
      by: <%= @spotlight.description%>
    </h2>
    <p class="spotlight__description">
     <strong><%= @spotlight.description%></strong>
    </p>
  </section>

  <hr class="root__hr" />
  <section class="top-ten__container">
    <section class="top-ten__list-container">
    <h2 class="top-ten__header">
        Top Movies
      </h2>
      <ul class =  "media-list">
      <% @movies.each do |work| %>
        <li>
          <%= link_to work.title, work_path(work)%> by <%= work.creator%>
        </li>
      <% end %>
      </ul>
    </section>
      <section class="top-ten__list-container">
        <h2 class="top-ten__header">
          Top Books
        </h2>
        <ul class =  "media-list">
          <% @books.each do |work| %>
            <li>
              <%= link_to work.title, work_path(work)%> by <%= work.creator%>
            </li>
          <% end %>
        </ul>
      </section>
        <section class="top-ten__list-container">
          <h2 class="top-ten__header">
            Top Albums
          </h2>
          <ul class =  "media-list">
            <% @albums.each do |work| %>
              <li>
                <%= link_to work.title, work_path(work)%> by <%= work.creator%>
              </li>
            <% end %>
          </ul>
        </section>
  </section>
</main>
